[project]
name = "forge-harness"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.5", "pyyaml>=6.0", "gitpython>=3.1", "asyncpg>=0.29", "fastapi>=0.110", "click>=8.1"]
[project.scripts]
forge = "forge.cli.forge:main"
[tool.ruff]
line-length = 100
[tool.mypy]
strict = true
