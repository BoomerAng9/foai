fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
openai==1.59.3
anthropic==0.42.0
PyYAML==6.0.2
python-jose[cryptography]==3.3.0
structlog==24.4.0
tenacity==9.0.0

# Wave 1 Step F — ReMe (AgentScope) for long-term agent memory.
# File-based ReMeLight backend by default; vector backend opt-in via env.
# Repo: https://github.com/agentscope-ai/ReMe (Apache-2.0, ~2.8k stars)
# Pip: pypi.org/project/reme-ai/
reme-ai>=0.3.1

# Wave 1 Step G — AgentScope multi-agent fan-out beneath squad mode.
# Default-off behind SQUAD_BACKEND env (asyncio | agentscope). Wraps the
# existing _dispatch_squad asyncio.gather() pattern with MsgHub primitives.
# Repo: https://github.com/agentscope-ai/agentscope (Apache-2.0)
agentscope==1.0.19
