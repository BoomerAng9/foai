title = "foai gitleaks configuration"

[extend]
useDefault = true

[[rules.allowlist]]
description = "Test fixture files — fake api_key values in pytest fixtures"
paths = [
    '''coastal-brewing/tests/.*\.py''',
    '''.*test.*\.py''',
    '''.*_test\.py''',
    '''.*conftest\.py''',
]
