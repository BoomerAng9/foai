{
  "name": "@aims/melanium",
  "version": "0.1.0",
  "description": "Melanium Ingot A2P currency — $0.99 Digital Maintenance Fee on every agent-to-agent transaction, split 70% to the ACHIEVEMOR vault + 30% to the customer platform currency balance. Customer 30% is the Savings Plan allocation. Charter shows generic 'Digital Maintenance Fee'; Ledger tracks full split.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./pricing": "./src/pricing.ts",
    "./client": "./src/client.ts",
    "./queries": "./src/queries.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aims/contracts": "*",
    "postgres": "^3.4.5",
    "zod": "^3.23.8"
  },
  "peerDependenciesMeta": {
    "postgres": {
      "optional": true
    }
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
