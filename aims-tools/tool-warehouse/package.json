{
  "name": "@aims/tool-warehouse",
  "version": "0.1.0",
  "description": "The canonical Tool Warehouse — 9-tier taxonomy, Neon-backed inventory, internal_only enforcement. Picker_Ang scans this catalog at RFP→BAMARAM Step 3 (Commercial Proposal) to produce the Bill of Materials + Security Addendum. Replaces the outdated Tool Warehouse v4 markdown doc with live data.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./client": "./src/client.ts",
    "./queries": "./src/queries.ts",
    "./enforcement": "./src/enforcement.ts",
    "./seed-tools": "./src/seed-tools.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "zod": "^3.23.8"
  },
  "peerDependenciesMeta": {
    "postgres": {
      "optional": true
    }
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
