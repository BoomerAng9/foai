-r requirements.txt

# Wire schema regeneration — run `python -m grpc_tools.protoc ...` from
# the command in runtime/spinner/_wire/__init__.py. Not required at
# production runtime; only for developer regeneration after schema
# changes.
grpcio-tools==1.80.0
mypy-protobuf==5.0.0

# Testing
pytest==8.3.3
