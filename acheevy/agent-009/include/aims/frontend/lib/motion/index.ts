/**
 * Motion System — A.I.M.S.
 *
 * Re-exports tokens and variants for clean imports:
 *   import { fadeUp, transition, spring } from '@/lib/motion';
 */

export * from "./tokens";
export * from "./variants";
